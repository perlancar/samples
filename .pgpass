# format:
#
# hostname:port:database:username:password
#
# some notes:
# - the character '*' can match any value in any of the fields except password
# - if PGPASSWORD is set, ~/.pgpass is not read
# - location can be customized via PGPASSFILE
